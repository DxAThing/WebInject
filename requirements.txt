# WebInject 数据集制备 + 训练流水线 - 依赖包列表
# ============================================================

# ======================= 数据集制备 =======================

# 大模型推理框架（本地 7B 模型 Prompt 生成）
transformers>=4.40.0
accelerate>=0.30.0

# Flash Attention 2（加速长文本推理，需要 CUDA）
# 安装方式: pip install flash-attn --no-build-isolation
flash-attn>=2.5.0

# 深度学习框架 (CUDA 12.6)
# 请使用以下命令安装 CUDA 版本，而非直接 pip install -r:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
torch>=2.0.0
torchvision>=0.15.0

# 网页解析（DOM 压缩器）
beautifulsoup4>=4.12.0

# 网页自动化和浏览器控制（截图生成 + 爬虫搜索）
selenium>=4.0.0

# 图像处理（截图渲染）
Pillow>=12.0.0

# 数学和数据处理
numpy>=1.20.0

# HTTP 请求（DuckDuckGo 搜索）
requests>=2.28.0

# ======================= 训练流水线 =======================

# 高性能键值数据库 (训练 I/O 优化)
lmdb>=1.4.0

# 注：以下为 Python 标准库，无需安装
# - os, json, io, base64, random, string, ast, glob, re
# - subprocess, signal, time, datetime, traceback

# 注：真实网页采集还需要 Node.js 工具 single-file-cli
# 安装方式: npm install -g single-file-cli
